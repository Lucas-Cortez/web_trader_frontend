// import { StateCreator } from "zustand";
// import { createStoreWithMiddleware } from "../createStoreWithMiddleware";
// // import { Order } from "@/entities/order";

// export type TestStore = {
//   count: number;
//   add: (n: number) => void;
// };

// export const testStore: StateCreator<TestStore, [], []> = (set) => ({
//   count: 1,
//   add: (n) => set((state) => ({ count: state.count + n })),
// });

// export const useTestStore = createStoreWithMiddleware(testStore);
